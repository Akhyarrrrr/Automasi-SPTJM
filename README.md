# SPTJM Otomatis (Streamlit)

## Fitur
- Upload Excel → Generate PDF SPTJM per orang (LibreOffice headless)
- Download ZIP semua PDF
- Download beberapa sample PDF untuk dicek
- (Opsional) Kirim email 1 PDF per pemilik (SMTP dari .env)
- Report Generate & Report Email (CSV)

## Install
```bash
python -m venv .venv
source .venv/Scripts/activate   # windows git bash
pip install -r requirements.txt
